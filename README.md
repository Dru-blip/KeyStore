# KeyStore

A simple, secure local password manager built with Python and NiceGUI.

## Overview

KeyStore is a desktop application securely stores your passwords in an encrypted vault on your local machine.
All credentials are encrypted before being written to disk.

## Requirements

- Python 3.13+

## Installation

```
uv sync
```

## Usage

Run the application:

```
uv run main.py
```
